# Production environment variables for Firebase Hosting deployment
# Replace the URL below with your actual Google Cloud Run backend URL after deployment

# Cloud Run backend URL (update this after deploying to Cloud Run)
# Get it by running: gcloud run services describe trading-platform-backend --region us-central1 --format 'value(status.url)'
VITE_API_URL=https://trading-platform-backend-REPLACE_ME.run.app
